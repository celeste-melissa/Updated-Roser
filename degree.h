#include <iostream>
#include <string>
#ifndef degree_h
#define degree_h
using namespace std;

    // degree Programs
    // define an enumarated data type and assign values
    // check requirement C
    //adding class to enum helps to make the values distinct to that specific enum
enum class DegreeProgram {SECURTIY, NETWORK, SOFTWARE, UNDECIDED};
#endif
